// import { fromJS } from 'immutable';
// import { selectOrganisationProfilePageDomain } from '../selectors';

describe('selectOrganisationProfilePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
